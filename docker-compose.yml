version: "3.3"
services:
  server:
    build:
      context: .
      dockerfile: config/docker/backend/Dockerfile
    command: ./manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - config/env/.env
